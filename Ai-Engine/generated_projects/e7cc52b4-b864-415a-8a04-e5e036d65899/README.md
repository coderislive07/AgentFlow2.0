# Portfolio Website

This is a portfolio website built with HTML, CSS, and JavaScript. It includes a responsive design, contact form, and project showcase.

## Installation

1. Clone the repository
2. Run `npm install`
3. Run `npm start` to start the development server

## Deployment

Run `npm run build` to create a production-ready build in the `dist` folder.